apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jamaica-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard   # uncomment and set for your cluster
